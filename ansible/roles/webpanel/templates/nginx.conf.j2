server {
    listen 80;
    server_name {{ panel_domain }};

    location / {
        root {{ panel_dest }}/build;
        try_files $uri /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:3000/;
    }
}
