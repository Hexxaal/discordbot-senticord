{
  "name": "senticord-panel",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-session": "^1.17.1",
    "passport": "^0.4.1",
    "passport-discord": "^0.1.4",
    "dotenv": "^10.0.0"
  }
}
